---
- name: Converge full scenario
  hosts: all
  become: false
  gather_facts: true
  roles:
    - { role: bootstrap, tags: base }
    - { role: packages, tags: packages }
    - { role: dotfiles, tags: dotfiles }
    - { role: neovim, tags: editor }
    - { role: nerd_fonts, tags: fonts }
    - { role: rust, tags: rust }